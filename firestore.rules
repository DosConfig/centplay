rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Test mode - allow all reads/writes (temporary for development)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
